@model Cinema.Models.Biglietti

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Biglietti</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id_biglietti" />
            <input type="hidden" asp-for="Id" />
            <input type="hidden" asp-for="Id_posti" />
            <div class="form-group">
                <label asp-for="Id_spettacolo" class="control-label"></label>
                <select asp-for="Id_spettacolo" class="form-control" asp-items="ViewBag.Id_film"></select>
                <span asp-validation-for="Id_spettacolo" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Torna alla lista</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
